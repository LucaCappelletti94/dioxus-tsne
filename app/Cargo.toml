[package]
name = "decompositions-app"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
publish = false

[dependencies]
dioxus = { workspace = true, features = ["web"] }
dioxus-decompositions = { path = ".." }
